Review the current pull request or the diff on the current branch.

Check for:
- Correctness bugs and logic errors
- Security issues (injection, auth, secrets in code)
- Missing error handling at system boundaries
- Test coverage gaps

Report findings grouped by severity: **critical**, **warning**, **suggestion**.
Keep the report concise — skip findings with obvious fixes already present in the diff.